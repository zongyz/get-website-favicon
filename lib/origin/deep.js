'use strict';

module.exports = async url=>{
    let icons = []
    return new Promise( async (resolve, reject)=>{
        //TODO: find more icon from root directory or main domain
        resolve(icons)
    })
}